using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CustomGameManager : MonoBehaviour
{
   /* // Reference to the WorldStates component attached to the GameObject
    public WorldStates worldStates;

    void Start()
    {
        // Get the WorldStates component from the GameObject
        worldStates = FindObjectOfType<WorldStates>();
        if (worldStates == null)
        {
            // If the WorldStates component is not found in the scene, you can create it here
            worldStates = new WorldStates();
        }

        // Set the initial wood count to 0 in the agent's inventory
        worldStates.SetState("wood", 0);

        Debug.Log("Wood State: " + worldStates.GetStates()["wood"]);
    }
   */
}
